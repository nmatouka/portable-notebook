{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The frontend is untrusted notebook content and makes no Tauri IPC calls, so it is granted no permissions (least privilege). The dialog/window APIs the app uses are invoked from Rust, which capabilities do not gate.",
  "windows": ["main"],
  "permissions": []
}

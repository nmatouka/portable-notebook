# Shell scripts must stay LF so they run under Windows git-bash in CI.
*.sh text eol=lf
# Data files read by those scripts must be LF too (curl rejects URLs with a CR;
# sha256sum -c needs clean paths).
external-urls.txt text eol=lf
external-sha256.txt text eol=lf
